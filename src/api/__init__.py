"""
src/api — FastAPI module cung cấp API cho chatbot tuyển sinh.

Endpoints:
    GET /api/subject-groups   — Danh sách khối thi
    GET /api/avg-score        — Điểm trung bình theo khối thi
    GET /api/majors           — Query ngành/trường theo khối thi
"""
